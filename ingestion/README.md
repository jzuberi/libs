# ingestion

A lightweight ingestion lifecycle library providing:

- fetch() to acquire new data
- optional approval flows
- apply_update() for processing
- destination abstraction for writing data
- simple logging utilities

Designed for subclassing and extension.
